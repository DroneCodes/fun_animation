import 'package:flutter/material.dart';
class SnowflakeWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Customize the snowflake appearance
    return const Icon(
      Icons.ac_unit,
      color: Colors.white,
      size: 20,
    );
  }
}